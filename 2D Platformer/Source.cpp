#include <SFML/Graphics.hpp>
#include "Engine.h"
int main()
{
    //Declare an instance of Engine
    Engine engine;
    //Start the engine
    engine.run();
    //quit in the usual wayy when the engine is stopped
    return 0;
}